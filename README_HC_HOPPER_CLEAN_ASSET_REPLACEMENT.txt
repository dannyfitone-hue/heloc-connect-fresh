HC HOPPER CLEAN ASSET REPLACEMENT

- Replaced the previous poorly background-removed mascot animation with the exact MOV supplied by the owner.
- Converted it to browser-compatible MP4 and WebM formats.
- Removed the audio track completely from both website files.
- Kept the existing right-side placement, soft gold glow, mobile side-by-side layout, margins, and three-service menu unchanged.

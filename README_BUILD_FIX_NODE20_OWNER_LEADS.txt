This package fixes the Vercel build issue by forcing Node.js 20, adds .npmrc, patches package-lock registry URLs, and keeps the owner/admin leads API route at app/api/owner/leads/route.ts.
After uploading to GitHub, redeploy in Vercel with Build Cache OFF.

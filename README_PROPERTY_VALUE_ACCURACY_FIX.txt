HELOC CONNECT PROPERTY VALUE ACCURACY FIX

Changes:
- Fixed a bug that treated a generic fallback as a successful ATTOM result.
- The app now checks all AVM address variations before using a fallback.
- Removed assessment and old-sale endpoints from the live market-value result.
- Added a more realistic fallback for ZIP 92673 (San Clemente).
- Property value remains editable by the homeowner.

No email, portal, lead, SMS, or calculator features were removed.
